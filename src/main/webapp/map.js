// Ejemplo de map: Duplicar cada número en un array
const numeros = [1, 2, 3, 4, 5];
const duplicados = numeros.map(numero => numero * 2);

console.log("Original:", numeros);
console.log("Duplicados:", duplicados);
